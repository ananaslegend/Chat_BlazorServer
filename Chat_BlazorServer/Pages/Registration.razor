@page "/registration"

<h3>Registration!</h3>

<EditForm Model="obj" OnSubmit="HandleRegistration">
    <div class="form-group" style="padding-bottom: 20px;">
        <label for="login">Your Login</label>
        <InputText id="login" class="form-controll" @bind-Value="login"/>
    </div>
    <div class="form-group" style="padding-bottom: 20px;">
        <label for="password">Password</label>
        <InputText id="password" class="form-controll" @bind-Value="password" type="password"/>
    </div>

    <button class="btn btn-primary" type="submit">Submit</button>
</EditForm>



@code
{
    object obj = new();
    string login;
    string password;
    private void HandleRegistration()
    { }


    @*User user = new User();

    private void HandleRegistration()
    {
        chatService.RegistrateUser(user);
        NavManager.NavigateTo("/login");
    }*@
}